APS Scheduling System
A lightweight production scheduling system built with Flask and ECharts. It provides an intuitive web interface for visualizing production schedules and resource allocation.

Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Run server
python app.py
```
